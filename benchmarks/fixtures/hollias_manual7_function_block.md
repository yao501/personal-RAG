# HOLLiAS MACS V6.5 用户手册 7（脱敏节选：功能块 / 术语）

## 术语：参数对齐

**参数对齐**：用于在下装时对比在线值与离线值，并提示用户是否进行同步。

- 当该属性为 **TRUE** 时：下装时系统会对比在线值与离线值，并提示用户是否同步。
- 当该属性为 **FALSE** 时：下装时不进行比较与同步提示。

> 注：本文件为脱敏节选，用于回归测试；不包含真实客户信息。
