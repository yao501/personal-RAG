## P0-B：真实 PDF Q8 topResults 观察（只看 Q8）

### 1) 检查 query

- `什么是参数对齐？`

### 2) topResults 观察摘要（top 5）

数据源：`evals/results/sprint-5.3c-realpdf-run-001.json`（本地真实 PDF 抽样 run 的 Q8）。

topResults（按顺序）：

1. **手册7 / `1.6 术语`**（主 citation；包含参数对齐定义 + TRUE/FALSE）
2. 手册7 / `5.2.9.7 工作模式`（非表头噪声，属于正文段落）
3. 手册7 / `5.2.9.4 详细说明`（非表头噪声，属于正文段落）
4. 手册4 / `5.18.1 配置`
5. 手册4 / `10.5 人机交互界面`

结论：**top1 稳定为“真实定义块/术语定义块”**，未被 `6.2.6.2 引脚` 这类表头型伪段落抢位。

### 3) 伪段落剩余形态

在本次 Q8 的 top 5 中，**未出现**明显目录型 / 表头型 / 短行字段拼接型伪段落。

（历史上 Q8 的 topResults 曾出现 `6.2.6.2 引脚` 等表头型候选；本次 top 5 已不再出现。）

### 4) 相较上一轮是否下降

判断：**明显下降**（以 topResults 结构为准）。

理由：过去观察到的表头型候选（如 `6.2.6.2 引脚`）会进入 topResults 并与定义块竞争；当前 Q8 的 topResults 前列被“术语定义 + 正文说明段”占据。

### 5) P0-B 是否建议先收口

结论：**A. 可以先收口 P0-B（针对 Q8 同族这一条线）**。

备注：收口不等于永远不做；若后续真实回归集中出现“表头噪声重新上位”的稳定复现，再以单点复现开小 PR 继续 ranking/parse_normalize 小步。

