# Q6 Follow-up（不阻塞 Sprint 5.3 关闭）

## 1. 为什么 Q6 不再阻塞 5.3 关闭

5.3c 真实 PDF 抽样已 **5/6 pass**（≥4 pass），阶段收尾线在结果文件中已满足（`evals/results/sprint-5.3c-realpdf-summary-001.md`）。**Q6 是唯一 partial**，属于**单题**上「控制器侧 / 工程总控侧」表述与证据命中的对齐问题，不改变「多卷真实 PDF 主链与关键题已收敛到可交付」的结论，故 **5.3 正式关闭**，Q6 **单独 follow-up**。

## 2. Q6 当前状态

- **真实 PDF（5.3c 抽样）**：**partial**（见 `sprint-5.3c-realpdf-summary-001.md`、`sprint-5.3c-realpdf-run-001.json`）。
- **主要问题**：gold/规则期望同时体现 **控制器侧** 与 **工程总控/站侧**；当前仍以 **工程总控 FAQ**（如「先编译后下装」类短段）为主，**控制器侧**要点未稳定命中或写入 direct。
- **合成侧提醒**（非本次结论重判）：5.3b 消融说明 B 组下 Q6 曾出现 **direct 与两阶段 gold 不一致**、规则 **假阳性** 风险（`sprint-5.3b-ablation-summary-001.md`）；后续修补时仍以 **人工对照 gold + model_answer** 为准。

## 3. Follow-up 定义（刻意收窄）

- **范围**：仅 **Q6**，或 **同类 2～3 个问法**（同一意图：编译/下装/顺序 + 两阶段边界）。
- **不做**：**不扩 benchmark**；**不重开大 sprint**；不借机上 **LLM judge** 或做大范围 UI/Agent 改造。

## 4. 若实施修补时的窄建议（可选）

1. **术语命中**：在「编译 / 下装 / 顺序」类 query 下，提高同时覆盖 **控制器侧** 与 **工程总控** 语义的片段排序或候选来源（小步，与现有分册/注入策略一致）。
2. **模板对齐**：两阶段 **direct** 与真实引文一致，避免只剩一句 FAQ。
3. **复测**：真实 PDF 上 **只跑 Q6 + 同类 2～3 问法**，确认 Q1/Q8 等已收敛题无回归。

可记为 backlog 或 **5.3d 级极小步**，默认 **不**与下一阶段 P0 抢主线，除非产品明确插队。
