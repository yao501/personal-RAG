# Q6 Follow-up（不阻塞 Sprint 5.3 关闭）

## 1. 为何 Q6 不再阻塞 Sprint 5.3 关闭

Sprint 5.3 的收口标准在 **5.3c 真实 PDF 抽样**上已达成：**5/6 pass**（≥4 pass），且 **Q1 / Q8** 等关键短板已按阶段目标收敛。**Q6 是唯一仍为 partial 的抽样题**，属于**单题表述与命中对齐**问题，不改变「多卷真实 PDF 上主链与检索已可交付」的整体结论，因此单独列为 **follow-up**，不用于继续拖住 5.3 阶段关闭。

## 2. Q6 当前状态

- **真实 PDF（5.3c 抽样）**：verdict **partial**（见 `evals/results/sprint-5.3c-realpdf-summary-001.md` 与 `sprint-5.3c-realpdf-run-001.json`）。
- **问题集中点**：gold 与规则检查要求同时覆盖 **「控制器侧」** 与 **「工程总控/站侧」** 两类表述；当前检索与答案仍以 **工程总控 FAQ**（如「先编译后下装」类短答）为主，**控制器侧**要点未稳定满足。
- **5.3b 消融中的提示**（合成侧）：关闭补块时 Q6 曾出现 **direct 与两阶段 gold 不一致**、**keyword 规则假阳性** 的风险说明（见 `evals/results/sprint-5.3b-ablation-summary-001.md`），真实 PDF 上需继续用 **人工对照 gold** 与 `model_answer` 校验，而非仅看 verdict。

## 3. 问题性质

- **小修补遗**：在已有检索与顺序模板框架上，收窄 **术语命中**（控制器侧 vs 工程总控侧）与 **两阶段顺序** 的表述对齐。
- **非阶段性阻塞**：不属于「必须再开一轮大 sprint 才能关 5.3」的那类问题；可在 **后续有空闲优先级时** 单独处理。

## 4. 若日后做窄范围修补（可选，非新大 sprint）

仅作 **非常窄** 的后续建议，**不**扩展题集、**不**引入 LLM judge、**不**做大重构：

1. **术语与检索**：在「编译 / 下装 / 顺序」类 query 下，提高 **同时包含「控制器侧」与「工程总控」语义** 的片段排序或命中（例如与现有分册偏置、补块策略一致的小步调整）。
2. **模板对齐**：与现有 **两阶段顺序** direct 模板对齐，确保 **阶段一（控制器）** 与 **阶段二（工程总控/站）** 在真实 PDF 引文上可对应，避免只留一句 FAQ。
3. **复测范围**：真实 PDF 上 **只复测 Q6**，或 **同类 2～3 个问法**，确认不再引入 Q1/Q8 等已收敛题的回归。

以上若实施，可记为 **5.3d 级小步** 或 **产品 backlog 条目**，**不必**默认立即开、也不必写成新的大 sprint 方案。
